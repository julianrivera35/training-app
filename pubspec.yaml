name: training_app
description: App de entrenamiento — Pivot Selección Bogotá
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  excel: ^4.0.6
  pdf: ^3.10.8
  printing: ^5.12.0
  file_picker: ^8.1.2
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  fl_chart: ^0.69.0
  intl: ^0.19.0
  share_plus: ^10.0.0
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
